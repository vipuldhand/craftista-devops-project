apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-catalogue-service
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-catalogue
  ports:
    - port: {{ .Values.catalogue.port }}
      targetPort: {{ .Values.catalogue.port }}